// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: study_session.sql

package db

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const abandonStudySession = `-- name: AbandonStudySession :one
UPDATE study_sessions SET
    status           = 'abandoned',
    finished_at      = NOW(),
    last_accessed_at = NOW()
WHERE session_id = $1
RETURNING session_id, user_id, deck_id, status, total_cards, completed_cards, last_completed_index, started_at, finished_at, last_accessed_at
`

func (q *Queries) AbandonStudySession(ctx context.Context, sessionID uuid.UUID) (StudySession, error) {
	row := q.db.QueryRowContext(ctx, abandonStudySession, sessionID)
	var i StudySession
	err := row.Scan(
		&i.SessionID,
		&i.UserID,
		&i.DeckID,
		&i.Status,
		&i.TotalCards,
		&i.CompletedCards,
		&i.LastCompletedIndex,
		&i.StartedAt,
		&i.FinishedAt,
		&i.LastAccessedAt,
	)
	return i, err
}

const countDeckLearners = `-- name: CountDeckLearners :one
SELECT COUNT(DISTINCT user_id) FROM study_sessions WHERE deck_id = $1
`

// Distinct users who have ever started a study session on this deck. Used by
// deck-service to show a "learners" count on the deck detail page. The deck
// owner studying their own deck is included.
func (q *Queries) CountDeckLearners(ctx context.Context, deckID uuid.UUID) (int64, error) {
	row := q.db.QueryRowContext(ctx, countDeckLearners, deckID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createStudySession = `-- name: CreateStudySession :one
INSERT INTO study_sessions (user_id, deck_id, total_cards)
VALUES ($1, $2, $3)
RETURNING session_id, user_id, deck_id, status, total_cards, completed_cards, last_completed_index, started_at, finished_at, last_accessed_at
`

type CreateStudySessionParams struct {
	UserID     uuid.UUID `json:"user_id"`
	DeckID     uuid.UUID `json:"deck_id"`
	TotalCards int32     `json:"total_cards"`
}

func (q *Queries) CreateStudySession(ctx context.Context, arg CreateStudySessionParams) (StudySession, error) {
	row := q.db.QueryRowContext(ctx, createStudySession, arg.UserID, arg.DeckID, arg.TotalCards)
	var i StudySession
	err := row.Scan(
		&i.SessionID,
		&i.UserID,
		&i.DeckID,
		&i.Status,
		&i.TotalCards,
		&i.CompletedCards,
		&i.LastCompletedIndex,
		&i.StartedAt,
		&i.FinishedAt,
		&i.LastAccessedAt,
	)
	return i, err
}

const finishStudySession = `-- name: FinishStudySession :one
UPDATE study_sessions SET
    status           = 'completed',
    finished_at      = NOW(),
    last_accessed_at = NOW()
WHERE session_id = $1
RETURNING session_id, user_id, deck_id, status, total_cards, completed_cards, last_completed_index, started_at, finished_at, last_accessed_at
`

func (q *Queries) FinishStudySession(ctx context.Context, sessionID uuid.UUID) (StudySession, error) {
	row := q.db.QueryRowContext(ctx, finishStudySession, sessionID)
	var i StudySession
	err := row.Scan(
		&i.SessionID,
		&i.UserID,
		&i.DeckID,
		&i.Status,
		&i.TotalCards,
		&i.CompletedCards,
		&i.LastCompletedIndex,
		&i.StartedAt,
		&i.FinishedAt,
		&i.LastAccessedAt,
	)
	return i, err
}

const getMostRecentSession = `-- name: GetMostRecentSession :one
SELECT session_id, user_id, deck_id, status, total_cards, completed_cards, last_completed_index, started_at, finished_at, last_accessed_at FROM study_sessions
WHERE user_id = $1
ORDER BY last_accessed_at DESC
LIMIT 1
`

func (q *Queries) GetMostRecentSession(ctx context.Context, userID uuid.UUID) (StudySession, error) {
	row := q.db.QueryRowContext(ctx, getMostRecentSession, userID)
	var i StudySession
	err := row.Scan(
		&i.SessionID,
		&i.UserID,
		&i.DeckID,
		&i.Status,
		&i.TotalCards,
		&i.CompletedCards,
		&i.LastCompletedIndex,
		&i.StartedAt,
		&i.FinishedAt,
		&i.LastAccessedAt,
	)
	return i, err
}

const getOngoingSessionByDeck = `-- name: GetOngoingSessionByDeck :one
SELECT session_id, user_id, deck_id, status, total_cards, completed_cards, last_completed_index, started_at, finished_at, last_accessed_at FROM study_sessions
WHERE user_id = $1 AND deck_id = $2 AND status = 'ongoing'
LIMIT 1
`

type GetOngoingSessionByDeckParams struct {
	UserID uuid.UUID `json:"user_id"`
	DeckID uuid.UUID `json:"deck_id"`
}

func (q *Queries) GetOngoingSessionByDeck(ctx context.Context, arg GetOngoingSessionByDeckParams) (StudySession, error) {
	row := q.db.QueryRowContext(ctx, getOngoingSessionByDeck, arg.UserID, arg.DeckID)
	var i StudySession
	err := row.Scan(
		&i.SessionID,
		&i.UserID,
		&i.DeckID,
		&i.Status,
		&i.TotalCards,
		&i.CompletedCards,
		&i.LastCompletedIndex,
		&i.StartedAt,
		&i.FinishedAt,
		&i.LastAccessedAt,
	)
	return i, err
}

const getStudySession = `-- name: GetStudySession :one
SELECT session_id, user_id, deck_id, status, total_cards, completed_cards, last_completed_index, started_at, finished_at, last_accessed_at FROM study_sessions WHERE session_id = $1
`

func (q *Queries) GetStudySession(ctx context.Context, sessionID uuid.UUID) (StudySession, error) {
	row := q.db.QueryRowContext(ctx, getStudySession, sessionID)
	var i StudySession
	err := row.Scan(
		&i.SessionID,
		&i.UserID,
		&i.DeckID,
		&i.Status,
		&i.TotalCards,
		&i.CompletedCards,
		&i.LastCompletedIndex,
		&i.StartedAt,
		&i.FinishedAt,
		&i.LastAccessedAt,
	)
	return i, err
}

const incrementCompletedCards = `-- name: IncrementCompletedCards :one
UPDATE study_sessions SET
    completed_cards      = completed_cards + 1,
    last_completed_index = last_completed_index + 1,
    last_accessed_at     = NOW(),
    status               = CASE WHEN completed_cards + 1 >= total_cards
                                THEN 'completed'::session_status
                                ELSE status
                           END,
    finished_at          = CASE WHEN completed_cards + 1 >= total_cards
                                THEN NOW()
                                ELSE finished_at
                           END
WHERE session_id = $1
RETURNING session_id, user_id, deck_id, status, total_cards, completed_cards, last_completed_index, started_at, finished_at, last_accessed_at
`

// Bumps the per-review counters and atomically flips the session to
// 'completed' on the final card so it can't get stuck in 'ongoing' state.
// A stuck ongoing-but-fully-graded session would cause the next StartSession
// to resume it and every /review call to 409 with ErrCardAlreadyReviewed.
func (q *Queries) IncrementCompletedCards(ctx context.Context, sessionID uuid.UUID) (StudySession, error) {
	row := q.db.QueryRowContext(ctx, incrementCompletedCards, sessionID)
	var i StudySession
	err := row.Scan(
		&i.SessionID,
		&i.UserID,
		&i.DeckID,
		&i.Status,
		&i.TotalCards,
		&i.CompletedCards,
		&i.LastCompletedIndex,
		&i.StartedAt,
		&i.FinishedAt,
		&i.LastAccessedAt,
	)
	return i, err
}

const listRecentDecks = `-- name: ListRecentDecks :many
SELECT DISTINCT ON (deck_id) deck_id, last_accessed_at
FROM study_sessions
WHERE user_id = $1
ORDER BY deck_id, last_accessed_at DESC
`

type ListRecentDecksRow struct {
	DeckID         uuid.UUID `json:"deck_id"`
	LastAccessedAt time.Time `json:"last_accessed_at"`
}

func (q *Queries) ListRecentDecks(ctx context.Context, userID uuid.UUID) ([]ListRecentDecksRow, error) {
	rows, err := q.db.QueryContext(ctx, listRecentDecks, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRecentDecksRow{}
	for rows.Next() {
		var i ListRecentDecksRow
		if err := rows.Scan(&i.DeckID, &i.LastAccessedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const topDecksByLearners = `-- name: TopDecksByLearners :many
SELECT deck_id, COUNT(DISTINCT user_id) AS learners
FROM study_sessions
WHERE last_accessed_at >= $1
GROUP BY deck_id
ORDER BY learners DESC, deck_id
LIMIT $2
`

type TopDecksByLearnersParams struct {
	LastAccessedAt time.Time `json:"last_accessed_at"`
	Limit          int32     `json:"limit"`
}

type TopDecksByLearnersRow struct {
	DeckID   uuid.UUID `json:"deck_id"`
	Learners int64     `json:"learners"`
}

// Ranks decks by the number of distinct users who had study activity within
// the trending window (last_accessed_at >= $1). deck-service then filters this
// list down to public, active decks. The deck owner's own activity is included.
func (q *Queries) TopDecksByLearners(ctx context.Context, arg TopDecksByLearnersParams) ([]TopDecksByLearnersRow, error) {
	rows, err := q.db.QueryContext(ctx, topDecksByLearners, arg.LastAccessedAt, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []TopDecksByLearnersRow{}
	for rows.Next() {
		var i TopDecksByLearnersRow
		if err := rows.Scan(&i.DeckID, &i.Learners); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
