// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: appeal.sql

package db

import (
	"context"
	"database/sql"

	"github.com/google/uuid"
)

const countDeckAppeals = `-- name: CountDeckAppeals :one
SELECT COUNT(*) FROM deck_appeals
WHERE status = COALESCE($1, status)
`

func (q *Queries) CountDeckAppeals(ctx context.Context, statusFilter NullAppealStatus) (int64, error) {
	row := q.queryRow(ctx, q.countDeckAppealsStmt, countDeckAppeals, statusFilter)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createDeckAppeal = `-- name: CreateDeckAppeal :one
INSERT INTO deck_appeals (
    token,
    deck_id,
    user_id,
    deck_name,
    moderation_reason
) VALUES (
    $1, $2, $3, $4, $5
)
RETURNING appeal_id, token, deck_id, user_id, deck_name, moderation_reason, status, user_message, submitted_at, decided_by, decision_note, decided_at, created_at, updated_at
`

type CreateDeckAppealParams struct {
	Token            string    `json:"token"`
	DeckID           uuid.UUID `json:"deck_id"`
	UserID           uuid.UUID `json:"user_id"`
	DeckName         string    `json:"deck_name"`
	ModerationReason string    `json:"moderation_reason"`
}

func (q *Queries) CreateDeckAppeal(ctx context.Context, arg CreateDeckAppealParams) (DeckAppeal, error) {
	row := q.queryRow(ctx, q.createDeckAppealStmt, createDeckAppeal,
		arg.Token,
		arg.DeckID,
		arg.UserID,
		arg.DeckName,
		arg.ModerationReason,
	)
	var i DeckAppeal
	err := row.Scan(
		&i.AppealID,
		&i.Token,
		&i.DeckID,
		&i.UserID,
		&i.DeckName,
		&i.ModerationReason,
		&i.Status,
		&i.UserMessage,
		&i.SubmittedAt,
		&i.DecidedBy,
		&i.DecisionNote,
		&i.DecidedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const decideDeckAppeal = `-- name: DecideDeckAppeal :one
UPDATE deck_appeals
SET status        = $2,
    decided_by    = $3,
    decision_note = $4,
    decided_at    = CURRENT_TIMESTAMP,
    updated_at    = CURRENT_TIMESTAMP
WHERE appeal_id = $1
  AND status = 'submitted'
RETURNING appeal_id, token, deck_id, user_id, deck_name, moderation_reason, status, user_message, submitted_at, decided_by, decision_note, decided_at, created_at, updated_at
`

type DecideDeckAppealParams struct {
	AppealID     uuid.UUID      `json:"appeal_id"`
	Status       AppealStatus   `json:"status"`
	DecidedBy    uuid.NullUUID  `json:"decided_by"`
	DecisionNote sql.NullString `json:"decision_note"`
}

func (q *Queries) DecideDeckAppeal(ctx context.Context, arg DecideDeckAppealParams) (DeckAppeal, error) {
	row := q.queryRow(ctx, q.decideDeckAppealStmt, decideDeckAppeal,
		arg.AppealID,
		arg.Status,
		arg.DecidedBy,
		arg.DecisionNote,
	)
	var i DeckAppeal
	err := row.Scan(
		&i.AppealID,
		&i.Token,
		&i.DeckID,
		&i.UserID,
		&i.DeckName,
		&i.ModerationReason,
		&i.Status,
		&i.UserMessage,
		&i.SubmittedAt,
		&i.DecidedBy,
		&i.DecisionNote,
		&i.DecidedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getDeckAppealByDeck = `-- name: GetDeckAppealByDeck :one
SELECT appeal_id, token, deck_id, user_id, deck_name, moderation_reason, status, user_message, submitted_at, decided_by, decision_note, decided_at, created_at, updated_at FROM deck_appeals
WHERE deck_id = $1
LIMIT 1
`

func (q *Queries) GetDeckAppealByDeck(ctx context.Context, deckID uuid.UUID) (DeckAppeal, error) {
	row := q.queryRow(ctx, q.getDeckAppealByDeckStmt, getDeckAppealByDeck, deckID)
	var i DeckAppeal
	err := row.Scan(
		&i.AppealID,
		&i.Token,
		&i.DeckID,
		&i.UserID,
		&i.DeckName,
		&i.ModerationReason,
		&i.Status,
		&i.UserMessage,
		&i.SubmittedAt,
		&i.DecidedBy,
		&i.DecisionNote,
		&i.DecidedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getDeckAppealByID = `-- name: GetDeckAppealByID :one
SELECT appeal_id, token, deck_id, user_id, deck_name, moderation_reason, status, user_message, submitted_at, decided_by, decision_note, decided_at, created_at, updated_at FROM deck_appeals
WHERE appeal_id = $1
LIMIT 1
`

func (q *Queries) GetDeckAppealByID(ctx context.Context, appealID uuid.UUID) (DeckAppeal, error) {
	row := q.queryRow(ctx, q.getDeckAppealByIDStmt, getDeckAppealByID, appealID)
	var i DeckAppeal
	err := row.Scan(
		&i.AppealID,
		&i.Token,
		&i.DeckID,
		&i.UserID,
		&i.DeckName,
		&i.ModerationReason,
		&i.Status,
		&i.UserMessage,
		&i.SubmittedAt,
		&i.DecidedBy,
		&i.DecisionNote,
		&i.DecidedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getDeckAppealByToken = `-- name: GetDeckAppealByToken :one
SELECT appeal_id, token, deck_id, user_id, deck_name, moderation_reason, status, user_message, submitted_at, decided_by, decision_note, decided_at, created_at, updated_at FROM deck_appeals
WHERE token = $1
LIMIT 1
`

func (q *Queries) GetDeckAppealByToken(ctx context.Context, token string) (DeckAppeal, error) {
	row := q.queryRow(ctx, q.getDeckAppealByTokenStmt, getDeckAppealByToken, token)
	var i DeckAppeal
	err := row.Scan(
		&i.AppealID,
		&i.Token,
		&i.DeckID,
		&i.UserID,
		&i.DeckName,
		&i.ModerationReason,
		&i.Status,
		&i.UserMessage,
		&i.SubmittedAt,
		&i.DecidedBy,
		&i.DecisionNote,
		&i.DecidedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listDeckAppeals = `-- name: ListDeckAppeals :many
SELECT appeal_id, token, deck_id, user_id, deck_name, moderation_reason, status, user_message, submitted_at, decided_by, decision_note, decided_at, created_at, updated_at FROM deck_appeals
WHERE status = COALESCE($3, status)
ORDER BY created_at DESC
LIMIT $1 OFFSET $2
`

type ListDeckAppealsParams struct {
	Limit        int32            `json:"limit"`
	Offset       int32            `json:"offset"`
	StatusFilter NullAppealStatus `json:"status_filter"`
}

func (q *Queries) ListDeckAppeals(ctx context.Context, arg ListDeckAppealsParams) ([]DeckAppeal, error) {
	rows, err := q.query(ctx, q.listDeckAppealsStmt, listDeckAppeals, arg.Limit, arg.Offset, arg.StatusFilter)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []DeckAppeal{}
	for rows.Next() {
		var i DeckAppeal
		if err := rows.Scan(
			&i.AppealID,
			&i.Token,
			&i.DeckID,
			&i.UserID,
			&i.DeckName,
			&i.ModerationReason,
			&i.Status,
			&i.UserMessage,
			&i.SubmittedAt,
			&i.DecidedBy,
			&i.DecisionNote,
			&i.DecidedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const submitDeckAppeal = `-- name: SubmitDeckAppeal :one
UPDATE deck_appeals
SET status       = 'submitted',
    user_message = $2,
    submitted_at = CURRENT_TIMESTAMP,
    updated_at   = CURRENT_TIMESTAMP
WHERE token = $1
  AND status = 'pending'
RETURNING appeal_id, token, deck_id, user_id, deck_name, moderation_reason, status, user_message, submitted_at, decided_by, decision_note, decided_at, created_at, updated_at
`

type SubmitDeckAppealParams struct {
	Token       string         `json:"token"`
	UserMessage sql.NullString `json:"user_message"`
}

func (q *Queries) SubmitDeckAppeal(ctx context.Context, arg SubmitDeckAppealParams) (DeckAppeal, error) {
	row := q.queryRow(ctx, q.submitDeckAppealStmt, submitDeckAppeal, arg.Token, arg.UserMessage)
	var i DeckAppeal
	err := row.Scan(
		&i.AppealID,
		&i.Token,
		&i.DeckID,
		&i.UserID,
		&i.DeckName,
		&i.ModerationReason,
		&i.Status,
		&i.UserMessage,
		&i.SubmittedAt,
		&i.DecidedBy,
		&i.DecisionNote,
		&i.DecidedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
